# Data Model: Contracts gRPC Package Cleanup

**Feature**: 005-contracts-grpc-cleanup
**Date**: 2026-03-27

## Summary

This feature involves no data model changes. The proto file (`physics_hub.proto`) and all generated types remain identical. The change is purely a build-time dependency swap in the `.csproj` file.

## Entities

No new, modified, or removed entities. The existing proto-generated types (messages, service stubs) are unchanged.
